export function formatUserData(data) {
    return {
        id: data.id,
        name: data.name,
        email: data.email,
        phone: data.phone,
        address: data.address,
        roles: data.roles,
        createdAt: data.createdAt,

    }

}